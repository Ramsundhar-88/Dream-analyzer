# Dream vs Reality - Backend

## Setup
cd backend
cp .env.example .env
# edit .env with your Mongo URI and (optionally) AI keys
npm install
npm run dev

Server will start at http://localhost:$PORT (default 5000).